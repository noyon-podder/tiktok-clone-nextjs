import { TMenuItems } from "@/app/types";
import React from "react";

const MenuItem = ({ iconString, colorString, sizeString }: TMenuItems) => {
  return <div>MenuItem</div>;
};

export default MenuItem;
